/*
    Working on Vectors
    Author: Sebastian Sroczyk
    Date: 03/06/2024
*/

#include <iostream>

// To use vecotrs we need to include the vector library (#include <vector>) 
#include <vector>

using namespace std;

int main(){

    // vectors are objets so they use a constructer to declare and initilize them.
    // Vectors also set themselves to 0 automaticly so we do not need to specify that.
    vector <char> (5);

    return 0;
}